/**
 * 
 * @param breaks 
 * @returns number
 * 
 * 
 */

export default function twoCrystalBalls(breaks: boolean[]): number {
  return -1;
}

const breaks = [false, false, true, false, false];
const result = twoCrystalBalls(breaks);
console.log(result); //
